# Clash Sentinel 实施步骤

本文将 [PROJECT_PLAN.md](PROJECT_PLAN.md) 转换为可以按顺序执行和验收的开发清单。首版目标是交付一个通过终端运行的 macOS 本机服务，而不是一次完成跨平台、后台自启动和全部原生重写。

## 一、技术基线

- **运行环境**：Node.js 当前 LTS、TypeScript、npm workspace。
- **后台和 API**：Koa、`@koa/router`、`koa-bodyparser`、`koa-static`。
- **接口校验**：Zod，并从 Schema 导出前后端共享的 TypeScript 类型。
- **前端**：React、Vite、React Router、TanStack Query。
- **本地存储**：SQLite、`better-sqlite3`、版本化数据库迁移。
- **测试**：Vitest、Supertest、Playwright。
- **代码质量**：ESLint、Prettier、TypeScript 类型检查。
- **启动方式**：开发环境执行 `npm run dev`，生产构建后执行 `npm start`。
- **监听地址**：固定为 `127.0.0.1`，不得监听全部网卡。
- **能力迁移**：首版封装并调用已验证的 `clash-entry-ip.sh`，稳定后再逐步迁移为 TypeScript。

## 二、流程精简原则

- `PROJECT_PLAN.md` 直接作为需求说明，不另写 PRD、立项书、竞品报告和用户画像。
- 保留低保真和高保真原型：低保真验证信息架构与操作路径，高保真验证最终视觉与交互。
- 不制作完整 Design System，只沉淀项目实际使用的颜色、间距和基础组件。
- 技术设计与低保真原型并行，高保真原型确认后再集中实现页面。
- 单机工具不建立测试、预发布和灰度三套环境，以自动测试、临时 Clash 配置和本机人工验收替代。
- 首版不规划云部署、用户体系、权限角色、埋点、运营指标和多租户。
- 每个步骤均须完成任务、交付物和验收条件，未通过当前步骤验收时不进入依赖它的后续步骤。

## 三、MVP 检查表

以下复选框表示功能实现完成情况，不表示需求已确认；Step 0 冻结范围后仍保持未勾选。

- [ ] 仅支持 macOS 与 Clash Verge Rev / Mihomo。
- [ ] 仅管理 Clash Verge Rev 当前选中的远程订阅。
- [ ] 仅处理单入口域名及其候选 IPv4，不切换代理节点或出口 IP。
- [ ] Web 服务仅允许本机访问。
- [ ] 自动切换默认关闭，由用户主动开启。
- [ ] 百度、淘宝、腾讯绕过代理，作为本机互联网基线。
- [ ] Google、GitHub、OpenAI 状态页经 Clash 代理探测，仅用于展示访问质量。
- [ ] OpenAI 官方总体状态与事故摘要独立展示。
- [ ] 所有配置修改遵循“校验、备份、修改、重载、验证、失败恢复”。
- [ ] 手工操作、定时任务和自动切换不能并发修改 Clash 配置。
- [ ] 首版通过终端运行，不实现 LaunchAgent、自启动和安装器。

## 四、安全不变量

以下规则贯穿所有步骤，不允许以后续优化为理由跳过：

1. 后端只监听 `127.0.0.1`。
2. API 不接收任意文件路径或任意 Shell 命令。
3. 外部命令使用 `spawn` 和参数数组执行，禁止拼接 Shell 字符串。
4. 只有当前有效诊断报告中标记合格的 IP 才能应用。
5. 应用前再次校验订阅 UID、原始文件指纹、报告有效期和候选资格。
6. 修改配置前创建备份，重载或冒烟验证失败时恢复原配置。
7. 所有配置修改任务全局串行，同一时刻最多执行一个。
8. 默认配置和正式验收环境中的页面、日志及 SQLite 不得输出或保存 Mihomo 密钥、完整订阅内容或不必要的本机路径；显式关闭脱敏仅限受控诊断，并由操作者承担终端与数据库明文泄露风险。
9. 本机互联网不可达或状态不确定时，不判断入口故障，也不自动切换。
10. Google、GitHub、OpenAI 的单站或多站异常不触发、不阻止自动切换。

### Step 0 冻结规则（2026-09-07）

- **首次使用**：未锁定入口显示“未锁定”，不累积入口故障次数；国内及海外站点继续监测。用户先诊断并确认应用合格 IP，完成当前订阅受管锁定后才可开启自动切换。不支持锁定时显示原因并保留站点监测。
- **检测默认值**：每 60 秒检测；国内三站至少两个可达才判断入口，入口连续失败 3 次触发严格诊断。互联网不可达或不确定时不增加入口失败次数。
- **自动处理条件**：已启用、已锁定、互联网正常、失败达到阈值、无操作冲突且冷却结束；实际应用还必须找到不同于当前 IP 的合格候选。
- **冷却**：默认 5 分钟，从每次自动处理结束计时，包含无候选、成功和已恢复的失败。期间继续监测，结束后故障持续则重新诊断；重启不重置冷却期限。
- **订阅变化**：UID 或原始文件指纹变化后清空失败计数、废弃旧报告并重读锁定状态。切换到其他订阅后自动切换需重新启用。
- **报告与确认**：报告默认有效期 30 分钟；apply 前复核有效期、UID、指纹及候选资格。apply、reset、rollback 均展示影响对象并确认，后台重新校验操作上下文。
- **调度与覆写**：配置修改以及覆盖共享报告或状态的 legacy 命令统一调度。仅处理已确认安全的透传模板或工具受管脚本，保留对自定义覆写的拒绝保护。
- **验证与恢复**：修改执行“校验 → 备份 → 修改 → 重载 → 独立冒烟验证”；失败恢复文件并重载。独立冒烟验证即使使用 Google，也不复用网站卡片监控结果。
- **恢复失败**：显示“需人工处理”，停止自动配置修改；不能仅凭文件恢复成功就报告回滚成功。
- **手动回滚**：仅允许上下文匹配的最近变更，订阅切换或相关配置被外部修改后拒绝旧快照覆盖。
- **解除锁定**：成功后关闭当前订阅自动切换，继续独立站点监测。

### 冻结场景表

| 场景 | 预期行为 | 配置修改边界 |
| --- | --- | --- |
| 首次未锁定 | 显示未锁定，站点照常检测，不累计入口故障 | 用户诊断并确认首次应用后才能启用自动切换 |
| 本机断网或不确定 | 暂停入口判断，不增加失败次数 | 不自动切换 |
| 单站或全部海外站失败 | 各卡片独立显示结果 | 不影响入口自动切换判定 |
| 入口连续故障 | 达到阈值后严格诊断 | 满足启用、锁定、冷却和互斥条件且有合格备选才应用 |
| 无合格候选 | 记录原因，进入冷却；故障持续时冷却后重试 | 保持原配置 |
| 订阅切换或更新 | 废弃报告、清空计数、重读锁定 | 跨订阅需重新启用，拒绝上下文不匹配的应用或回滚 |
| 解除锁定成功 | 关闭当前订阅自动切换，站点继续监测 | 保留独立监测能力 |
| 恢复失败 | 显示需人工处理并记录真实结果 | 停止自动配置修改 |

## 五、详细实施步骤

### Step 0：冻结 MVP 和安全边界

**前置依赖**：无。

**任务**：

- 对照 `PROJECT_PLAN.md` 检查本文件中的 MVP 清单、状态定义和自动切换条件。
- 固定配置修改、备份、验证和恢复顺序。
- 固定国内互联网基线与海外展示型监控的职责边界。
- 将不进入首版的需求统一放入“暂缓功能”，避免开发中临时扩展范围。

**交付物**：

- 本文的 MVP 检查表。
- 安全不变量。
- 暂缓功能清单。

**验收条件**：

- [x] 本文与 `PROJECT_PLAN.md` 不存在功能和默认值冲突。
- [x] 自动切换的触发、阻止和恢复条件完整。
- [x] 配置写入规则不需要开发阶段临时决策。

文档验收日期：2026-09-07。仅确认需求与安全边界已冻结，后续功能实现及测试仍按各步骤验收。

### Step 1：制作低保真原型

**前置依赖**：Step 0。

**任务**：

- 梳理单页仪表盘的信息架构：
  - 当前订阅、入口域名、锁定 IP 和入口状态。
  - 百度、淘宝、腾讯组成的国内互联网基线。
  - Google、GitHub、OpenAI 三张并列访问质量卡片。
  - OpenAI 官方总体状态和事故摘要。
  - 候选 IP、最近事件、策略设置和任务进度。
- 绘制核心操作流程：刷新已有状态、立即检测、重新诊断、选择并应用 IP、解除锁定、回滚和开启自动切换。
- 为加载、空数据、正常、失败、未知、过期和任务执行中状态设计线框。
- 明确 apply、reset、rollback 和开启自动切换的确认内容。

**交付物**：

- [低保真原型文档](docs/design/low-fidelity.md)：含桌面、窄屏、设置、确认弹窗及状态 SVG 图稿。
- 页面线框图和用户操作流程图。
- 页面状态清单与确认文案草案。

**验收条件**：

- [x] “刷新状态”与“立即检测”在位置和文案上不会混淆。
- [x] 国内互联网、入口健康、代理网站质量和 OpenAI 官方状态分层展示。
- [x] 所有修改配置的操作均明确展示影响对象和风险。
- [x] 页面在无诊断报告、未锁定入口和 Clash 未启动时仍有可理解的状态。

交付状态：静态图与文档自检完成，**用户已认可交付验收**，布局与操作路径已确认，可作为后续高保真原型的依据。

### Step 2：建立工程和质量基线

**前置依赖**：Step 0；可以与 Step 1 并行。

**任务**：

- 建立 npm workspace：
  - `apps/server`：Koa 后台服务。
  - `apps/web`：React + Vite 页面。
  - `packages/shared`：Zod Schema、API 类型和状态枚举。
  - `scripts/legacy`：受控保存基线 Shell 脚本。
- 配置 TypeScript、ESLint、Prettier、Vitest 和 Playwright。
- 提供统一命令：
  - `npm run dev`
  - `npm run build`
  - `npm start`
  - `npm test`
  - `npm run test:e2e`
  - `npm run lint`
  - `npm run typecheck`
- 配置 `.gitignore`，排除数据库、运行状态、日志、报告、备份和构建产物。
- 配置 GitHub Actions，执行格式检查、类型检查、测试和构建。

**交付物**：

- 可启动的 Koa API 与 React 空页面。
- 前后端共享类型包。
- 本地开发命令和持续集成配置。

**验收条件**：

- [x] 一条命令可以启动前后端开发环境。
- [x] 生产构建后的前端资源可以由 Koa 托管。
- [x] 全新检出仓库后可以按 README 完成安装、构建和测试。
- [x] CI 中所有基础检查通过。

本地验证（2026-09-08）：Node.js 24.2.0 / npm 11.3.0，`npm ci` 重新安装成功；格式、lint、类型检查、4 个单元及接口测试、生产构建和 2 个 Chromium 端到端测试通过。开发端口 5173 的 API 转发、3000 同源生产服务、端口占用报错及退出释放已验证。可复现安装通过清理依赖的 `npm ci` 验证，未另建 Git 检出目录。

实际工程为 `apps/server`、`apps/web`、`packages/shared`，`scripts/legacy` 仅保留说明。根命令包括 `dev`、`build`、`start`、`test`、`test:e2e`、`lint`、`typecheck`、`format` 和 `format:check`，详见 README。前端和测试工具统一使用 Vite 7；共享包先构建后使用。

远端 CI 状态（2026-09-08）：`main` 分支提交 `448f90d` 的“质量基线”工作流已通过，`npm ci`、格式检查、lint、类型检查、单元及接口测试、生产构建和 Chromium 端到端测试全部成功。业务 MVP 检查项仍未完成。

### Step 3：封装现有 Shell 能力

**前置依赖**：Step 2。

**任务**：

- 将基线项目中已验证的 `clash-entry-ip.sh` 引入 `scripts/legacy`，不引入 Android 脚本和无关工具。
- 建立适配接口：`getStatus`、`diagnose`、`healthCheck`、`applyIp`、`resetLock`、`rollback`。
- 使用 `spawn` 和参数数组运行脚本，为每类命令设置超时、退出码映射和异常终止处理。
- 解析诊断 TSV、监控状态和控制台输出，将其转换为类型化领域对象。
- 保存脱敏后的任务输出用于排障；API 不返回控制密钥、完整路径和订阅敏感内容。
- 首版不调用交互式 `switch`，手动切换采用“诊断 → 页面确认 → apply 指定合格 IP”。
- 使用临时目录、模拟 Clash 配置和伪造系统命令建立测试夹具。

**交付物**：

- Legacy adapter。
- TSV 和命令输出解析器。
- 模拟配置、命令夹具及适配层测试。

**验收条件**：

- [x] 成功、跳过、无候选、报告过期、订阅变化、应用失败和回滚均映射为稳定结果。
- [x] 测试不会读取或修改用户真实 Clash 配置。
- [x] 任意 API 输入不能转化为额外 Shell 参数或命令。
- [x] 子进程超时或异常退出后不会遗留运行中的修改任务。

本地验证（2026-09-08）：Legacy 脚本与基线仓库提交 `f20177e` 内容一致；适配层使用固定命令和参数数组启动独立进程组，覆盖 10 秒至 180 秒的分命令超时、温和终止及强制终止。14 个单元及接口测试和 2 个 Chromium 端到端测试通过；测试夹具全部位于临时目录，并验证诊断、健康、应用、重置、回滚、失败恢复、恶意参数、输出脱敏和超时后代进程清理。格式、lint、类型检查和生产构建通过。

### Step 4：建立数据模型和本地存储

**前置依赖**：Step 2；接口设计需参考 Step 3 的领域结果。

**任务**：

- 使用 SQLite 保存用户策略、当前健康快照、站点历史、候选 IP、诊断摘要、操作任务和事件记录。
- 用户策略包含检测间隔、请求超时、入口失败阈值、自动切换冷却时间、监测开关和自动切换开关。
- 状态枚举至少包含：
  - `healthy`
  - `internet_uncertain`
  - `internet_down`
  - `entry_suspected`
  - `entry_down`
  - `proxy_error`
  - `unknown`
- 每个站点结果保存目标标识、可达性、HTTP 状态码、HTTP 总耗时、错误类型和检测时间。
- 使用版本化迁移初始化数据库，并为站点历史和普通事件设置保留上限。

**交付物**：

- SQLite schema、迁移和数据访问层，以及[数据库设计文档](docs/database-design.md)。
- 前后端共享的领域类型。
- 数据模型与迁移测试。

**验收条件**：

- [x] 服务重启后策略、快照、任务和事件能够恢复。
- [x] 自动切换默认值为关闭。
- [x] 历史清理不会删除当前快照和最近关键事件。
- [x] 数据库和日志中不保存 Mihomo 明文密钥或完整订阅内容。

本地验证（2026-09-08）：使用 `better-sqlite3` 完成版本化迁移、外键、WAL、事务化快照与历史、最近诊断候选、任务生命周期及事件存储。临时数据库测试覆盖迁移幂等与失败回滚、重启恢复和运行中任务中断、默认策略、外键拒绝、数量清理及递归敏感数据过滤；24 个单元及接口测试和 2 个 Chromium 端到端测试通过。格式、lint、类型检查、生产构建及 `git diff --check` 均通过。

### Step 5：定义并实现 Koa API 骨架

**前置依赖**：Step 3、Step 4。

**只读接口**：

- `GET /api/status`
- `GET /api/sites`
- `GET /api/candidates`
- `GET /api/events`
- `GET /api/settings`
- `GET /api/tasks/:id`

**操作接口**：

- `POST /api/actions/health-check`
- `POST /api/actions/diagnose`
- `POST /api/actions/apply`
- `POST /api/actions/reset`
- `POST /api/actions/rollback`
- `PUT /api/settings`

**统一约定**：

- 使用 Zod 校验路径、查询参数、请求体和配置值。
- 采用统一成功与失败响应结构。
- 长操作立即返回任务 ID，由客户端查询任务状态。
- 五类 Legacy 动作全局串行；冲突请求返回 HTTP `409`。
- apply 仅接受当前有效诊断报告中的合格 IP。
- Koa 统一处理错误、请求日志、超时和前端静态资源。

**交付物**：

- [API 设计文档](docs/api-design.md)。
- Koa 中间件、路由、控制器和任务服务。
- 共享 API Schema。
- Supertest 接口测试。

**验收条件**：

- [x] 每个接口均覆盖成功、参数错误、业务拒绝和内部错误。
- [x] 刷新页面或重复读取不会触发写操作或新检测。
- [x] 重复提交修改操作不会重复修改配置。
- [x] API 不能利用参数访问允许目录之外的路径。

本地验证（2026-09-08）：API 文档在实现前完成并通过链接、Prettier 和空白检查；格式、lint、类型检查、30 个 Vitest/Supertest/Legacy/存储测试、生产构建和 2 个 Chromium 端到端测试通过。只读接口不调用 LegacyAdapter、不新增任务或事件；动作并发互斥、严格 IPv4 与未知字段拒绝、任务终态、快照刷新、事件分类及脱敏边界均由隔离测试覆盖。

### Step 6：实现定时健康检测

**前置依赖**：Step 3、Step 4、Step 5。

**任务**：

- 使用服务内调度器按默认 60 秒周期运行，同类检测任务不可重入。
- 绕过所有代理并行探测百度、淘宝和腾讯：至少两个成功为互联网可达，一个成功为状态不确定，全部失败为互联网不可达。
- 互联网可达时，通过 adapter 检测当前锁定入口的代表端口并维护连续失败次数。
- 当前入口连续失败达到默认 3 次后执行严格诊断。
- 经 Clash 本地代理并行探测 Google、GitHub 和 OpenAI 状态页，统一记录可达性、HTTP 状态码和完整 HTTP 总耗时。
- 额外解析 OpenAI 官方总体状态和当前事故摘要；无法获取或解析时记为 `unknown`。
- 为每项结果保存独立时间戳，超过两个检测周期未更新即标记为过期。

**交付物**：

- 调度器和检测编排服务。
- 国内互联网、入口和代理站点探测器。
- OpenAI 状态解析器。
- 健康状态转换与持久化测试。

**验收条件**：

- [x] 本机断网或状态不确定时不评价入口 IP。
- [x] 单个国内站点失败不会误判为断网。
- [x] 单个或全部海外站点失败不会增加入口失败次数。
- [x] Clash 未运行、入口失败、站点超时和 OpenAI 状态未知均能独立表达。
- [x] 一轮慢检测未完成时不会启动下一轮。

本地验证（2026-09-09）：使用 `undici` 完成显式直连与 Clash 代理六站探测，使用 `yaml` 从固定运行配置读取代理端口；手动与定时检测复用同一编排器和全局执行槽，定时轮次不写任务表。隔离测试覆盖国内 `3/3、2/3、1/3、0/3`、入口失败阈值及诊断读取、单站与全部代理故障、配置缺失、HTTP 超时、OpenAI 状态映射、动态过期、立即首跑、关闭监测、慢轮次防重入和双向冲突。格式、lint、类型检查、53 个 Vitest/Supertest/Legacy/存储测试、生产构建、27 个 API JSON Schema 示例和 2 个 Chromium 端到端测试通过。

### Step 7：制作高保真可交互原型

**前置依赖**：Step 1；示例数据结构参考 Step 4、Step 5。

**任务**：

- 根据低保真结果确定视觉层级、颜色、间距、字体和响应式布局。
- 制作 Google、GitHub、OpenAI 三张并列卡片，统一展示图标、状态、HTTP 状态码、延时和更新时间。
- 在 OpenAI 卡片中额外展示官方服务状态和事故摘要入口。
- 完成当前入口、候选 IP、事件列表、策略设置和任务进度区域。
- 覆盖正常、警告、失败、未知、过期、加载中、操作执行中、操作成功和失败后已恢复状态。
- 完成 apply、reset、rollback 和启用自动切换的确认弹窗。
- 验证桌面宽屏和窄屏布局。

**交付物**：

- [高保真可交互原型](docs/design/high-fidelity/README.md)。
- [桌面 1440px](docs/design/high-fidelity/screenshots/desktop.png)和[窄屏 390px](docs/design/high-fidelity/screenshots/mobile.png)截图。
- 页面状态稿和项目实际使用的简化视觉规范。

**验收条件**：

- [x] 原型使用接近真实 API 结构的示例数据。
- [x] 三个代理站点可以直观横向比较。
- [x] 危险操作不会因按钮层级或文案造成误触。
- [x] 高保真原型可以直接作为 React 实现和 Playwright 验收基准。

本地验证（2026-09-09）：独立静态原型覆盖 20 个可切换场景、四类具名确认、定时监测状态条、六站本地 Logo、设置抽屉、任务阶段以及 1440px / 390px 响应式布局。当前使用候选与推荐候选具有独立视觉语义。完整示例数据通过共享 Zod Schema 校验；原型专项测试、全部单元及接口测试和 Chromium 端到端测试通过。格式、lint、类型检查、生产构建和空白检查通过；原型不连接真实 API、网络、SQLite 或 Legacy Shell，实时调度状态的后端只读契约已纳入待实现 Step 8。

### Step 8：扩展定时监测状态 API

**前置依赖**：Step 5、Step 6、Step 7。

**任务**：

- 按 [API 设计文档](docs/api-design.md)为共享包增加监测状态 Schema 和响应类型，固定使用 `waiting | running | disabled`。
- 让 `HealthScheduler` 记录最近开始时间、最近完成时间和实际安排的下一次执行时间，并提供只读运行态快照。
- 新增 `GET /api/monitoring` 控制器，只读取当前设置和调度器内存状态。
- 不新增 SQLite 表或迁移，不持久化进程调度时间，也不把定时轮次写入任务表。
- 将三种状态、首次启动空时间、实际调度时间和读取无副作用纳入共享 Schema、调度器及接口测试。

**交付物**：

- 监测状态共享 Schema 与响应类型。
- 调度器只读运行态快照。
- `GET /api/monitoring` 控制器及测试。

**验收条件**：

- [x] `waiting`、`running`、`disabled` 和首次启动空时间均有稳定响应。
- [x] `nextRunAt` 取自调度器真实定时器，不由前端根据检测间隔推算。
- [x] 查询接口不会执行检测、创建任务或事件。
- [x] 服务重启后从本次进程生命周期重新建立状态，不伪造上一次进程的内存调度时间。

本地验证（2026-09-09）：共享包新增定时监测状态及响应 Schema，调度器以进程内时钟记录最近开始、最近完成和真实下一轮计划时间，`GET /api/monitoring` 只读取调度器快照。测试覆盖首次启动、等待、运行、暂停、运行中关闭、手动任务占槽、整轮失败和停止清理；查询接口不会调用检测或 Legacy 能力，也不会创建任务或事件。格式、lint、类型检查、57 个 Vitest/Supertest/Legacy/存储测试、生产构建、6 个完整 Playwright 测试、4 个原型专项测试和空白检查通过。

### Step 9：实现 SSE 状态通知通道

**前置依赖**：Step 5、Step 6、Step 8。

**任务**：

- 新增同源 `GET /api/stream`，使用 `text/event-stream` 建立只读长连接。
- 定义共享 SSE 通知 Schema，包含事件版本、进程内事件 ID、发生时间、原因和受影响资源；资源覆盖 `monitoring`、`status`、`sites`、`candidates`、`events`、`settings` 和指定任务。
- 增加进程内事件发布器，在定时检测开始和完成、设置更新、任务状态变化及业务快照更新后发布失效通知。
- 新连接立即发送一次全量同步通知；断线重连后重新读取权威快照，不依赖事件补发，也不持久化 SSE 事件。
- 每 15 秒发送无业务含义的保活消息，连接关闭时及时移除订阅者；单个慢连接或发送失败不得影响检测、任务和其他客户端。
- SSE 接口只通知数据发生变化，不执行网络检测、不创建任务、不写入 SQLite。
- 增加连接建立、通知路由、保活、断开清理、多客户端隔离和服务关闭测试。

**交付物**：

- 进程内状态事件发布器及共享通知类型。
- `GET /api/stream` SSE 接口。
- 服务端 SSE 生命周期与集成测试。

**验收条件**：

- [x] 状态变化后客户端能够收到对应资源的失效通知。
- [x] 新连接和重连后能够通过现有 GET API 得到完整一致快照。
- [x] 空闲连接由保活维持，断开的客户端不会泄漏监听器。
- [x] SSE 查询不会触发检测、任务或数据库写入。
- [x] SSE 客户端异常不会阻塞后台调度和手动操作。

本地验证（2026-09-09）：共享包新增版本化 SSE 失效通知 Schema，服务端新增进程内通知中心及 `GET /api/stream`，支持新连接全量同步、15 秒注释保活、无历史补发、慢连接隔离和关闭清理。健康检查返回内部变化摘要，定时检测与手动任务据此精确发布实际变化资源，候选仅在诊断成功替换后失效；普通设置更新不发布 SSE，`reset` 成功会额外失效设置资源。SSE 长连接不受普通 API 10 秒超时限制。格式、lint、类型检查、65 个 Vitest/Supertest/Legacy/存储测试、生产构建、6 个完整 Playwright 测试、4 个原型专项测试和空白检查通过。

### Step 10：实现只读 Web 看板

**前置依赖**：Step 5 至 Step 9。

**任务**：

- 按高保真原型实现 React 单页仪表盘。
- 使用 TanStack Query 处理 API 请求与缓存，通过 SSE 失效通知驱动对应查询刷新。
- 建立统一类型安全的只读 API 请求层；健康探针使用 3 秒超时，普通快照读取使用 12 秒超时以保留后端 10 秒统一超时响应，SSE 长连接不设置请求超时。
- 页面启动时并行读取现有 GET 快照；连续相同通知在同一渲染周期内合并，只失效对应 Query。
- SSE 正常时停止固定业务轮询；连接失败时每 15 秒低频刷新看板快照，并按 1、2、5、10、30 秒上限指数退避重连。
- SSE 恢复后立即全量校准并停止兜底轮询；服务重启导致事件 ID 重置时不补放旧事件。
- 页面不可见时保留 SSE 连接但不执行兜底轮询，重新可见时立即校准。
- 本步骤的页面自动刷新和用户刷新都只读取已有快照；“立即检测”等写操作仅保留禁用入口，在 Step 12 中启用。
- 展示当前订阅和入口、国内互联网基线、三个代理站点、OpenAI 官方状态、候选 IP 和最近事件。
- 按独立时间戳显示新鲜、过期或未知状态，候选诊断从保存时间起固定 30 分钟有效。
- 实现加载、空数据、接口错误和后台不可达提示；后台不可达时旧数据仅供参考，并统一标为离线或过期。

**交付物**：

- React 看板与可复用状态组件。
- API 请求层、SSE 通讯层和页面状态测试。
- Playwright 只读流程、SSE 在线、断线降级与重连校准测试。

**验收条件**：

- [x] 页面布局和状态表现与高保真原型一致。
- [x] Google、GitHub、OpenAI 并列显示且可以比较。
- [x] 页面自动刷新不会发起新网络探测。
- [x] 后台不可用时明确提示，不继续把旧数据展示为健康。
- [x] SSE 通知只刷新受影响数据，断线时自动降级为低频轮询，恢复后停止轮询。
- [x] 多个页面、重复通知和服务重启不会造成重复探测、请求风暴或状态不一致。

本地验证（2026-09-10）：React 看板按高保真原型实现入口、定时监测、六站状态、候选、事件和只读设置抽屉；统一 API 请求层使用健康探针 3 秒、普通快照 12 秒的分层超时并保留结构化后端错误。SSE 通讯层支持精确 Query 失效、同周期合并、1、2、5、10、30 秒退避重连、断线 15 秒轮询、页面可见性校准和多页面隔离；全部写操作入口保持禁用。格式、lint、类型检查、78 个 Vitest/Supertest/Legacy/存储测试、生产构建、9 个完整 Playwright 测试和 4 个原型专项测试通过。

### Step 11：完善服务端日志系统

**前置依赖**：Step 2、Step 4 至 Step 10。

**任务**：

- 使用 Winston Console Transport 建立统一自然文本日志，覆盖 HTTP、SSE、任务、检测、调度、Legacy、SQLite 及启动关闭生命周期。
- 日志使用本地时间、`DEBUG`、`INFO`、`WARN`、`ERROR` 四个等级、稳定模块名和 `key=value` 元数据；完整输出 `requestId`、`taskId` 和 `runId`。
- 开发命令移除继承的 `NO_COLOR` 并设置 `FORCE_COLOR=1`；生产启动仍按环境自动判断颜色。
- 统一以仓库根目录解析 SQLite、Legacy 脚本、状态和报告路径，保证 `npm run dev` 与 `npm start` 行为一致。
- 新增根目录 `config/default.yaml`，分别配置日志与 SQLite 脱敏，默认均开启。
- 支持 `CLASH_SENTINEL_CONFIG` 指定替代配置；严格校验配置，加载失败时安全记录并以退出码 1 结束。
- SQLite 脱敏开关只影响新写入，不迁移或改写历史数据；关闭脱敏仍保留 Schema、序列化、大小和状态转换约束。

**交付物**：

- Winston 自然文本日志及关键业务日志。
- 日志颜色、等级、模块和安全格式化能力。
- 服务端 YAML 配置加载器和默认配置。
- 统一运行路径解析与启动生命周期。
- 日志、配置及 SQLite 脱敏测试。
- README、API 和数据库设计说明。

**验收条件**：

- [x] HTTP、SSE、任务、健康检测、调度、Legacy、SQLite 和服务生命周期均有对应日志。
- [x] 默认日志与 SQLite 写入均执行脱敏，两个开关可独立配置。
- [x] 配置缺失、非法或包含未知字段时服务拒绝启动。
- [x] 关联 ID 完整输出，开发日志显示等级和模块颜色。
- [x] 开发与生产启动使用相同仓库根目录，不在 workspace 下误建数据库、日志或报告。
- [x] 日志异常不影响业务流程，SQLite 关闭脱敏后仍执行全部非脱敏安全校验。
- [x] `SIGINT`、`SIGTERM` 和启动失败均能正确关闭运行时资源。

本地验证（2026-09-11）：服务端使用 Winston Console Transport 输出自然文本日志，覆盖 HTTP、SSE、任务、健康检测、调度、Legacy、SQLite 和启动关闭生命周期；关联 ID 保持完整，开发命令强制启用等级与模块颜色。运行路径由模块位置稳定定位仓库根目录，开发与生产启动行为一致。根目录严格 YAML 配置分别控制日志和 SQLite 脱敏，默认开启且互不影响；存储开关只影响新写入，不改写历史内容。格式、lint、类型检查、94 个 Vitest/Supertest 测试、生产构建和 13 个 Playwright 测试通过；`npm start`、缺失配置失败退出、开发彩色日志、优雅关闭和端口释放均已真实验证。

### Step 12：实现手动操作闭环

**前置依赖**：Step 5、Step 10、Step 11。

**任务**：

- 实现立即检测和重新诊断。
- 实现选择合格 IP、展示影响对象、二次确认和 apply。
- 实现 reset 与 rollback 的影响说明和二次确认。
- 开放定时监测、检测间隔、请求超时、失败阈值和冷却时间编辑；自动切换保持只读并留到 Step 13。
- 前端优先根据 SSE 的指定任务失效通知刷新任务进度；断线时才轮询 `/api/tasks/:id`，任务进入终态后立即停止，且禁止重复提交。
- 任务终态保留到用户关闭，并通过当前标签页 `sessionStorage` 在刷新后恢复。
- 后端执行前重新校验候选资格、报告有效期、订阅 UID 和文件指纹。
- 持久化配置任务失败后的 `recoveryStatus`，展示成功、拒绝、失败、已恢复和人工处理建议。

**交付物**：

- 手动操作页面和确认弹窗。
- 修改任务队列与状态查询。
- 手动操作集成测试和端到端测试。

**验收条件**：

- [x] 诊断、应用、解除锁定和回滚形成完整闭环。
- [x] 手动操作与定时任务不会并发修改配置。
- [x] 页面刷新不会重复执行最近操作。
- [x] apply 失败后能够区分已恢复、恢复失败和未知状态，不会误报成功恢复。
- [x] 监测设置可校验并保存，自动切换设置在 Step 13 前保持不可编辑。

本地验证（2026-09-11）：五类手动动作已接入 TanStack Query Mutation、指定任务 Query 与 SSE 精确失效；SSE 断线时任务每 2 秒轮询，终态停止并保留到用户关闭，刷新页面从 `sessionStorage` 恢复但不会重复提交。配置动作恢复状态已加入共享 Schema、SQLite v2 迁移、Legacy 适配层、任务审计和页面提示；只有文件还原与 Mihomo 重载均成功才标记 `recovered`。监测设置开放编辑和边界校验，保存响应直接更新缓存并刷新调度状态，自动切换仍保持只读。格式、lint、类型检查、101 个 Vitest/Supertest 测试、生产构建、4 个原型 Playwright 测试和 15 个完整 Playwright 测试通过。

### Step 13：实现自动切换

**前置依赖**：Step 6、Step 12。

**启动条件**：

- 国内互联网基线正常。
- 当前入口端口连续失败达到阈值。
- 严格诊断找到不同于当前地址的合格候选 IP。
- 自动切换已由用户主动开启，且绑定 UID 与当前订阅一致。
- 当前不在冷却期。
- 没有其他配置修改任务运行。

**执行流程**：

```text
重新确认互联网基线
→ 严格诊断
→ 校验订阅和报告
→ 创建备份
→ 应用候选 IP
→ 重载 Mihomo
→ 执行既有代理冒烟验证
→ 成功记录事件或失败恢复
```

**边界处理**：

- Google、GitHub、OpenAI 的结果不触发、不阻止自动切换。
- 订阅 UID 或文件指纹变化后，废弃报告并清空失败计数。
- 没有合格候选时只记录和提示，不在同一冷却窗口内反复诊断。
- 冷却期内继续监测，但不再次修改配置。
- 恢复失败、恢复状态未知或自动任务被服务重启中断时，关闭自动切换并清空绑定 UID。

**交付物**：

- 自动切换状态机。
- 冷却和订阅变化处理。
- 自动切换事件记录与故障注入测试。

**验收条件**：

- [x] 真实入口故障且存在候选时可以自动恢复。
- [x] 本机断网、报告过期、订阅变化、无候选和冷却期内不会误切换。
- [x] 切换验证失败后恢复原配置并记录完整事件。
- [x] 服务重启后不会绕过冷却期立即重复切换。

本地验证（2026-09-11）：手动“立即检测”和定时检测已共用自动切换状态机，健康任务持久化完成后继续在同一操作租约中执行自动后置阶段。每次真正进入自动处理都会创建 `auto_switch` 任务，并统一记录 queued、running、成功、无不同候选、失败、恢复状态、审计事件及 SSE 资源；冷却从自动任务终态开始持久化。健康命令按当前设置动态传入失败阈值，订阅 UID 或原始文件指纹变化会清空失败计数和旧诊断，跨订阅时同时安全关闭自动切换。恢复失败、状态未知以及服务重启遗留的排队中或运行中自动任务都会关闭开关并清空绑定 UID。Web 已开放独立确认的自动开关、绑定 UID、冷却倒计时和自动任务刷新恢复，并保持未保存普通设置草稿隔离。格式、lint、类型检查、110 个 Vitest/Supertest 测试、生产构建、4 个原型 Playwright 测试和 17 个完整 Playwright 测试通过。

### Step 14：任务架构重构与目录整理

**前置依赖**：Step 13。

Step 13 完成后，围绕任务执行入口、健康检查与自动切换职责继续进行了以下重构与稳定性修复：

1. **2026-09-12 00:40**（`c6bdeb2`）：将单体任务服务拆分为可扩展的 `TaskEngine`，建立任务契约、Handler 注册表和独立任务 Handler。
2. **2026-09-12 22:19**（`c19d7f5`）：将定时健康检查统一接入 `TaskEngine` 的瞬时执行入口；定时 `health_check` 根任务不落库，触发的 `auto_switch` 继续作为持久化任务执行。
3. **2026-09-12 22:28**（`bf15684`）：将持久化后续任务入口收为引擎内部方法，把 `followUps` 统一更名为 `nextTasks`，并将自动切换触发判断归入 `HealthCheckService`。
4. **2026-09-12 23:23**（`98638b4`）：删除独立的 `OperationCoordinator`，由 `TaskEngine` 私有管理全局操作槽、活动操作身份和令牌保护；Scheduler 不再取得或传递租约。
5. **2026-09-12 23:48**（`8b7459e`）：将具体任务 Handler 及其测试整理到 `tasks/handlers/`，并把通用 helpers 拆分为配置任务支持和 Legacy 失败转换两个职责明确的模块。
6. **2026-09-13 09:57**（`a16c6f0`）：在 HTTP 后台任务链边界捕获后续任务创建等链级异常，记录任务与请求上下文、终止当前链并释放全局操作槽，避免业务存储异常升级为进程退出。
7. **2026-09-13 10:14**（`4d3c15d`）：将关停信号改为持续监听；优雅关停期间再次收到 `SIGINT` 或 `SIGTERM` 时，分别以退出码 `130` 或 `143` 立即强制退出。
8. **2026-09-13 10:29**（`a20963a`）：修正 bodyparser 及其他中间件产生的客户端错误映射，非 JSON API 请求体返回 `415 UNSUPPORTED_MEDIA_TYPE`，其他有效 4xx 返回 `INVALID_REQUEST`，不再误报内部错误。
9. **2026-09-13 11:10**（`2c5984a`）：将按 URL 无上限缓存的 `ProxyAgent` Map 收敛为单个当前代理连接池；代理地址变化时异步回收旧实例，关停时等待当前及待回收实例完成清理。
10. **2026-09-13 11:16**（`1da0ef0`）：由 `freshness.ts` 中的 `CANDIDATE_FRESHNESS_MS` 统一控制诊断报告 30 分钟有效期，消除有效性判断与界面到期时间的重复魔法数字。

**最终架构**：

- 手动任务与定时健康检查共用 `TaskEngine`、任务契约和 Handler 注册表。
- 手动健康检查创建并持久化 `health_check` 任务；定时健康检查使用瞬时根任务，不写入任务表，不发送通用 `task_*` 通知，也不追加通用任务审计。
- 定时健康检查触发的 `auto_switch` 仍作为持久化任务执行，保留完整任务生命周期、配置动作审计和失败恢复信息。
- `TaskEngine` 私有管理单个进程内全局操作槽，并在同一执行链中将活动身份切换到持久化的下一任务；`HealthScheduler` 只负责定时控制、监测通知和运行时间状态。
- Handler 只依赖最小任务执行身份，通过 `nextTasks` 声明成功后需要顺序执行的后续任务。
- 健康检查与自动切换触发判断由 `HealthCheckService` 统一完成，自动切换 Handler 只负责执行前复核、配置修改、冷却和失败恢复。
- HTTP 后台任务链在引擎边界隔离链级异常，单次后续任务存储失败不会触发进程级 `unhandledRejection`；首次关停保持优雅流程，二次关停信号提供确定的强制退出路径。
- API 中间件产生的客户端错误保持 4xx 语义；非 JSON 请求体和其他无法处理的客户端请求使用稳定共享错误码。
- `UndiciSiteProbe` 仅保留当前 Clash 代理连接池，代理地址切换和服务关停都会回收相关 Agent；诊断报告新鲜期由前端单一常量定义。

**验收条件**：

- [x] 手动任务与定时健康检查保持全局互斥，冲突响应和活动任务信息不变。
- [x] 定时 `health_check` 不落库，手动 `health_check` 与自动 `auto_switch` 保持持久化。
- [x] 瞬时健康检查、持久化下一任务、资源汇总和失败释放操作槽均有测试覆盖。
- [x] Scheduler 不再直接依赖健康检查实现、自动切换规划器或独立操作协调器。
- [x] 任务 Handler 目录和共享辅助模块按职责完成整理。
- [x] 后台任务链异常被记录并终止当前链，操作槽始终释放且引擎可以继续接受新任务。
- [x] 二次 `SIGINT` 和 `SIGTERM` 分别立即以 `130` 和 `143` 退出，不再重复等待异步清理。
- [x] bodyparser 与其他中间件产生的 4xx 保持客户端错误响应，不会降级为 `500 INTERNAL_ERROR`。
- [x] 代理地址变化时旧连接池被回收，关停会等待全部当前及待回收代理连接池。
- [x] 诊断有效性判断与界面到期时间共用 `CANDIDATE_FRESHNESS_MS`。

本地验证（2026-09-13）：完整 138 个 Vitest/Supertest 测试、TypeScript 类型检查、ESLint、Step 14 文档 Prettier 检查和 `git diff --check` 均通过。上述修复未修改数据库结构、HTTP 响应结构、SSE 数据结构或自动切换安全边界；API 仅新增稳定的 `INVALID_REQUEST` 与 `UNSUPPORTED_MEDIA_TYPE` 客户端错误码。

### Step 15：继续项目重构

**前置依赖**：Step 14。

**状态**：已完成。

Step 14 完成后，继续围绕存储职责、自动切换策略和共享契约边界完成以下重构：

1. **2026-09-13 16:17**（`7812a01`）：将单体 SQLite Store 拆分为连接、编解码及 settings、health、site、diagnosis、task、event 领域仓储；启动恢复从存储层上移为独立服务，由运行时显式编排，并同步整理测试支持和数据库设计文档。
2. **2026-09-13 17:59**（`d5180b9`）：将自动切换准入判断和候选选择集中到纯策略模块，健康检查只负责采集事实，自动切换服务负责执行计划，避免相同条件分散在多个服务中。
3. **2026-09-13 19:06**（`1b30bdb`）：将自动切换策略、服务及测试整理到 `services/auto-switch/`，统一相关引用，使目录结构与领域职责一致。
4. **2026-09-13 19:13**（`1a41238`）：将 691 行共享 Schema 入口按 common、health、settings、diagnosis、task、event、api 七个领域拆分；根入口继续使用 `export *` 转发，保持现有包导入方式和 101 个公共导出不变。

**最终架构**：

- SQLite 连接和 JSON/值编解码作为基础设施，由六个领域仓储分别管理各自查询、写入和清理规则；聚合 Store 仅负责初始化、事务和生命周期。
- 启动恢复由服务层协调任务中断、自动切换关闭和审计记录，不再作为存储层的隐式副作用。
- 自动切换准入条件和候选决策集中在可独立测试的纯策略模块；健康检查、自动切换执行和任务 Handler 保持单向职责协作。
- 自动切换实现集中在独立目录，调用方通过稳定模块路径引用策略与服务。
- shared 包内部依赖保持 `api → domain → common`，领域模块不反向依赖 API 或根入口；外部继续通过 `@clash-sentinel/shared` 根入口消费契约。

**交付物**：

- 按领域拆分的 SQLite 仓储、公共编解码模块和独立启动恢复服务。
- 自动切换纯策略模块、领域目录和对应测试。
- 按七个领域拆分的共享 Schema、根入口兼容层和分域测试。
- 同步更新的数据库设计文档和测试支持代码。

**验收条件**：

- [x] 存储层按领域拆分，事务、初始化和关闭行为保持稳定。
- [x] 启动恢复由运行时显式编排，遗留任务和不安全自动切换状态仍按既定规则处理。
- [x] 自动切换准入与候选策略只有一个实现来源，并具有独立边界测试。
- [x] 自动切换服务目录与领域职责一致，调用路径完成统一。
- [x] shared 根入口的 101 个公共导出无新增或缺失，服务端和 Web 无需修改导入方式。
- [x] 数据库表结构、HTTP 响应结构、SSE 数据结构及自动切换安全边界保持不变。

本地验证（2026-09-13）：完整 161 个 Vitest/Supertest 测试、TypeScript 类型检查、shared 文件 Prettier 检查和 `git diff --check` 均通过。生产构建和 Playwright 验收留待 Step 20 统一执行。

### Step 16：引入 TSyringe 重构运行时依赖装配

**前置依赖**：Step 15。

**状态**：未开始。

使用 TSyringe 建立服务端 IoC 容器，以 `ApplicationRuntime` 作为唯一应用根对象，取代当前集中手工构造依赖的 `runtime.ts`。本步骤只重构依赖装配和生命周期，不改变数据库、HTTP API、SSE、任务执行及自动切换行为。

#### 子步骤 16.1：建立 TSyringe 基础设施

- 服务端加入 `tsyringe` 和 `reflect-metadata`，开启 `experimentalDecorators` 与 `emitDecoratorMetadata`。
- 在服务端入口最先加载 `reflect-metadata`，确保使用装饰器的 ESM 模块求值前已初始化反射元数据。
- 新增集中 token 定义，为 `AppLogger`、`ProcessEnv`、`ServerConfig`、运行路径、时钟、任务 Handler 注册表等编译后不存在的接口或值类型提供唯一 `Symbol`。
- 新增应用容器工厂；TSyringe 默认容器只作为父容器和元数据入口，业务模块不得导入或调用默认 `container`。
- 每次调用容器工厂都创建 child container，并在其中注册环境、配置、日志器及服务实现。
- 进程级服务统一注册为 `Lifecycle.ContainerScoped`，不使用 `@singleton()`，保证生产容器内单例且不同测试容器之间不共享状态。

#### 子步骤 16.2：改造构造函数注入

- 领域服务和任务 Handler 使用 `@injectable()`，具体类依赖由 TSyringe 根据构造函数类型自动装配。
- 将 `AutoSwitchService`、`HealthCheckService`、`HealthScheduler`、`TaskEngine` 等 options 对象构造函数改为逐项构造函数注入。
- `AppLogger`、配置、运行路径、时钟和精简能力接口使用 `@inject(TOKENS.xxx)`。
- 保留现有 `Pick` 能力边界；必要时为 Handler 使用的仓储能力定义独立 token，避免为了自动装配而扩大依赖权限。
- `SqliteStore`、`LegacyAdapter`、`ClashProxyConfig`、`StatusNotificationCenter` 等包含配置转换、默认值或特殊参数的对象通过容器工厂 provider 创建。
- 任务 Handler 继续组成完整、不可变的 `TaskHandlerRegistry`；注册表由容器工厂生成并注入 `TaskEngine`，不在 `TaskEngine` 内动态访问容器。
- `createApp()` 和 `createApiRouter()` 继续接收明确依赖，不在 Koa 中间件、Router、服务、仓储或 Handler 中调用 `container.resolve()`。

#### 子步骤 16.3：建立唯一应用根和生命周期

- 新增 `ApplicationRuntime`，由容器注入 Store、自动切换服务、任务引擎、调度器、通知中心、探测器、配置和日志器。
- `index.ts` 创建生产 child container，并且只解析一次 `ApplicationRuntime`；之后所有调用均为普通对象方法调用。
- `ApplicationRuntime.start()` 按既定顺序执行启动恢复、创建 Koa 应用、监听本地端口和启动调度器。
- `ApplicationRuntime.stop()` 保留现有关闭顺序：停止接收任务、停止调度器、关闭 SSE、停止 HTTP 服务、等待任务空闲、释放探测器和数据库。
- 保留启动恢复失败时立即关闭已创建资源的行为，保证半初始化容器不会遗留 SQLite 或 Undici 句柄。
- 容器 `dispose()` 只释放由容器持有且实现 `Disposable` 的最终资源；业务停机顺序仍由 `ApplicationRuntime.stop()` 显式控制。
- 删除被取代的 `RuntimeDependencies`、`createRuntimeDependencies()` 和 `runtime.ts`，避免同时维护手工组合根与 IoC 组合根。

#### 子步骤 16.4：测试隔离与架构约束

- 单元测试继续优先直接构造被测服务，不要求通过容器运行。
- 新增测试容器工厂；每个需要完整依赖图的集成测试创建独立 child container，覆盖配置、日志器、内存数据库和 Legacy mock，并在结束时调用 `dispose()`。
- 添加容器冒烟测试，验证 `ApplicationRuntime`、全部任务 Handler 和关键服务可以成功解析，同一 child 内 `ContainerScoped` 对象保持相同实例。
- 添加容器隔离测试，验证两个 child container 不共享 Store、Notifier、Scheduler、TaskEngine 或其他可变服务。
- 添加依赖覆盖测试，验证测试 child 中注册的 mock 不污染父容器和其他测试。
- 添加生命周期测试，覆盖正常启动关闭、启动恢复失败、监听失败、二次关闭和资源释放顺序。
- 增加静态架构检查，限制 TSyringe 的 `container`/`resolve()` 只出现在 composition、入口和容器测试中。
- 更新服务端相关测试中的构造参数，确保原有业务测试断言保持不变。

**交付物**：

- TSyringe 服务端依赖及 TypeScript 装饰器配置。
- 集中 token、应用容器工厂和生产/测试 child container。
- 使用构造函数注入的领域服务与任务 Handler。
- 唯一应用根 `ApplicationRuntime` 及显式启动、停机编排。
- 容器解析、隔离、覆盖和生命周期测试。
- 删除原 `runtime.ts` 后的服务端组合根结构说明。

**验收条件**：

- [ ] `index.ts` 只从应用 child container 解析 `ApplicationRuntime`，业务模块不直接访问容器。
- [ ] 所有进程级可变服务使用 `ContainerScoped`，未使用会跨 child 共享实例的 `@singleton()`。
- [ ] 两个独立 child container 的 Store、Notifier、Scheduler 和 TaskEngine 实例互不共享。
- [ ] 全部任务 Handler 可由容器组成完整注册表，任务类型覆盖保持不变。
- [ ] 启动恢复及优雅关闭顺序与重构前一致，失败时不遗留数据库、HTTP、SSE、定时器或网络连接。
- [ ] 单元测试可继续绕过容器直接构造服务，集成测试可通过 child container 覆盖依赖。
- [ ] `runtime.ts`、`RuntimeDependencies` 和手工依赖装配被删除，不存在第二套组合根。
- [ ] 数据库结构、共享 Schema、HTTP 响应、SSE 数据和自动切换安全边界无变化。
- [ ] 全量 Vitest/Supertest 测试、类型检查、ESLint、Prettier、生产构建和 `git diff --check` 通过。

**假设与默认选择**：

- 采用“装饰器优先”方案：业务类使用 `@injectable()`，接口和值使用 `@inject(Symbol token)`。
- 采用 `ApplicationRuntime` 类作为唯一应用根。
- 生命周期集中注册为 `ContainerScoped`，不使用 `@singleton()` 和业务模块自注册。
- 不启用文件扫描或基于 glob 的自动发现；模块通过正常 ESM import 加载，容器集中注册生命周期和特殊 provider。
- 不将 TSyringe 容器注入业务对象，不引入 Service Locator。
- 本步骤属于内部架构重构，不新增配置项、接口字段、数据库迁移或用户可见功能。

### Step 17：全面完善服务端设计文档

**前置依赖**：Step 16。

**状态**：待规划。

**任务、交付物与验收条件**：待后续细化。

### Step 18：全面完善前端设计文档

**前置依赖**：Step 17。

**状态**：待规划。

**任务、交付物与验收条件**：待后续细化。

### Step 19：根据前端设计文档重构前端页面

**前置依赖**：Step 18。

**状态**：待规划。

**任务、交付物与验收条件**：待前端设计文档完善后细化。

### Step 20：终端运行与 MVP 验收

**前置依赖**：Step 0 至 Step 19。

**任务**：

- 复核 `.env.example`、`config/default.yaml` 和配置风险说明，不保存真实密钥。
- 支持 `npm run dev`、`npm run build` 和 `npm start`。
- 终端启动时检查 Node.js 版本、端口占用、Clash 配置和 Mihomo 控制接口。
- 捕获 `SIGINT` 和 `SIGTERM`，停止调度器、等待不可中断的恢复步骤完成并安全关闭数据库。
- 服务关闭时停止接收 SSE 连接、释放全部订阅者并安全结束长连接；验证断线客户端可以退避重连并恢复快照。
- 在真实 Clash Verge Rev 环境完成一次人工验收。
- 执行断网、入口失败、无候选、重载失败和订阅变化故障注入。
- 更新 README 的安装、运行、使用、风险和停止说明。
- 生成版本号、发布说明和已知限制，创建首个 GitHub Release。

**交付物**：

- 可通过终端运行的 MVP。
- 人工验收记录和故障注入结果。
- 完整 README 和发布说明。

**验收条件**：

- [ ] 终端运行期间服务与页面稳定可用。
- [ ] 按 `Ctrl+C` 可以安全退出。
- [ ] SSE 长连接可稳定保活，断线后能够降级刷新并在服务恢复后重新同步。
- [ ] 全量测试、构建、类型检查和格式检查通过。
- [ ] 关键失败路径不会留下半修改配置。
- [ ] 发布包不包含数据库、日志、报告、备份和敏感配置。

## 六、暂缓功能

以下内容不进入首版：

- LaunchAgent 和用户登录自启动。
- 安装器、菜单栏应用和系统托盘。
- 将 legacy Shell 全量重写为 TypeScript。
- 延时历史趋势图。
- 系统通知和其他通知渠道。
- 多订阅和多设备管理。
- Windows、Linux 和 Android 支持。
- 公网访问、用户登录和远程控制。

终端运行版稳定后，再将 LaunchAgent 作为独立里程碑规划，避免首版同时处理核心业务、Web 页面和 macOS 服务生命周期。

## 七、Definition of Done

每个 Step 完成时必须同时满足：

- [ ] 功能或文档交付物已完成。
- [ ] 对应自动测试通过。
- [ ] 错误、空状态和边界情况已处理。
- [ ] 默认配置和正式验收环境未泄露本机敏感路径、Mihomo 密钥和订阅内容；关闭脱敏仅用于受控诊断。
- [ ] README、接口说明或相关设计文档已同步。
- [ ] Git 差异中不包含无关改动。

## 八、实施纪律

- 每个功能 Step 至少使用一个独立提交；同一 Step 内的连续重构可以按职责拆分为多个提交，并在步骤记录中按提交时间说明演进顺序和验收结果。
- 未完成 Step 6 的状态分类与故障测试前，不开发自动切换。
- 配置写入逻辑必须先在临时配置和模拟 Mihomo 环境中通过测试，再进行真实环境验收。
- 原型阶段发现的信息架构问题优先修正文档，不在页面实现后继续堆叠临时状态。
- Legacy adapter 保持边界清晰；新增业务规则写入 Node.js 服务，不继续扩大 Shell 脚本职责。
- 任何改变自动切换条件或配置恢复策略的需求，必须同步更新 `PROJECT_PLAN.md`、本文件和对应测试。

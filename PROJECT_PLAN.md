# Clash Sentinel（Clash 哨兵）项目规划

## 一、项目定位

基于 [`clash-network-test`](https://github.com/soapgu/clash-network-test) 的诊断与入口锁定能力，建设一个集定期健康检测、故障自动切换和 Web 管理于一体的本机服务。

项目仓库名称：`clash-sentinel`。

首版支持 macOS + Clash Verge Rev / Mihomo，仅管理当前订阅。“切换 IP”指切换同一入口域名的候选 IPv4，不是切换代理节点或更换出口 IP。

## 二、核心功能

### 1. 定期健康检测

- 后台定期检测本机互联网、当前入口 IP、代理实际访问和外部服务状态是否正常。
- 以绕过代理访问百度、淘宝和腾讯的结果作为“本机互联网基线”，用于区分本机断网、入口故障、代理异常和状态不确定，避免将所有失败都归因为 IP 故障。
- 经 Clash 本地代理对 Google、GitHub 和 [OpenAI 官方状态页](https://status.openai.com/)发起轻量 HTTP 探测，分别记录可达状态、HTTP 状态码、最近检测时间和 HTTP 请求总耗时（毫秒）。总耗时覆盖 Clash 转发、DNS、TCP/TLS 和服务响应，不等同于候选入口 IP 的纯 TCP 建连耗时。
- 经 Clash 本地代理访问 [OpenAI 官方状态页](https://status.openai.com/)，记录状态页是否可达、官方总体运行状态和当前事故摘要。无法获取或解析官方状态时显示“未知”，不得据此推断为 OpenAI 故障或 Clash 故障。
- OpenAI 官方状态属于跨服务、套餐和错误类型的聚合结果，可能与具体账号、模型或功能的实际可用性不同；首版不使用 API Key 测试 OpenAI API、ChatGPT 登录状态或特定模型。
- 为各检测目标分别记录最近结果、独立时间戳和有限历史数据，支持识别过期数据并为后续延时趋势图提供数据。
- 记录当前入口的连续失败次数；Google、GitHub 和 OpenAI 状态页的结果不计入该次数。
- 支持配置检测间隔、超时和失败阈值；默认每 60 秒检测，连续失败 3 次后触发故障诊断。

### 2. 故障自动切换

- 继承现有候选 IP 发现、代表端口抽样和多轮检测能力。
- 本机互联网正常且确认入口故障后，自动寻找并应用合格的备用 IP。
- 切换前备份，切换后重载配置并验证代理可用性；失败时恢复原配置。
- 提供自动切换开关、冷却机制和操作互斥，防止频繁切换或手动操作与后台任务冲突。
- 无合格候选、本机断网或故障原因不明确时，不盲目切换，保留状态与原因。
- 当前订阅切换或更新后，重新识别入口并废弃旧诊断结果。
- 自动切换仍以本机互联网正常、当前入口端口连续故障和候选 IP 严格诊断结果为依据。Google、GitHub 或 OpenAI 状态页单站失败不触发、不阻止入口 IP 切换，也不改变切换后的既有代理冒烟验证标准。

### 3. Web 状态监控与手动操作

- **状态总览**：展示当前订阅、入口域名、锁定 IP、健康状态、最近检测时间和自动切换开关；将“本机互联网”“Clash 代理站点”和“OpenAI 官方服务状态”分区展示，避免合并成单一健康结论。
- **网站监控**：以并列卡片展示 Google、GitHub 和 OpenAI 状态页的可达状态、HTTP 状态码与 HTTP 总耗时，支持直观对比三个站点的访问质量；探测失败时展示错误状态，不生成虚假延时。
- **服务状态**：在 OpenAI 访问质量卡片之外，额外展示其官方总体状态和当前事故摘要；官方状态不可用时展示“未知”。
- **候选 IP**：展示候选地址、检测结果、耗时与推荐情况。
- **手动操作**：立即健康检查、重新诊断、切换至合格 IP、解除锁定、撤销最近变更。
- **策略设置**：调整检测间隔、失败阈值和切换冷却时间，暂停或恢复监测。
- **事件记录**：查看故障、切换、验证失败与回滚历史。
- 页面自动更新状态；“刷新状态”仅更新展示，“立即检测”才发起新检测。配置变更类操作需明确确认。

## 三、系统组成与首版边界

- **后台服务**：负责定时检测、诊断、故障判定和自动恢复。
- **Clash 适配层**：负责读取当前订阅、维护入口锁定、配置重载及回滚，沿用现有安全边界。
- **管理接口与 Web 页面**：提供状态查询、策略配置、手动操作和操作进度查询；本阶段不限定技术栈及具体接口路径。
- **本地存储**：保存策略、检测摘要、事件与恢复所需备份，敏感配置不进入页面和普通日志。

首版仅提供本机访问的 Web 管理，不开放公网控制；自动切换由用户主动启用。暂不包含 Android 自动控制、跨平台适配、多设备管理、节点优选、订阅购买或通用 Clash 配置编辑。

## 四、阶段与验收

1. **监测与可视化**：完成后台检测、状态存储及只读看板；能区分互联网、入口和代理异常。
2. **手动管理闭环**：完成诊断、切换、解除锁定和回滚；页面能展示执行进度与结果。
3. **自动恢复闭环**：完成故障阈值、自动切换、冷却及安全保护；服务可随用户登录启动。

关键验收场景：

- 正常网络持续监测，不误触发切换。
- 本机断网或网络状态不确定时，不切换入口 IP。
- 国内站点正常而 Google、GitHub 或 OpenAI 状态页单站失败时，只标记对应站点异常，不增加入口连续失败次数。
- Google、GitHub 和 OpenAI 状态页探测成功时，以并列卡片分别显示 HTTP 总耗时；失败时显示错误状态而非虚假延时。
- 本机断网时，不因海外站点全部失败而自动切换入口 IP。
- OpenAI 官方发生事故时，展示官方总体状态与事故摘要；状态页不可达或无法解析时显示“未知”。
- 各类检测结果使用独立时间戳，页面能够识别过期数据，不把历史成功结果当作当前状态。
- 入口连续故障且存在合格备选时，自动切换并验证成功。
- 无备选、重载失败或验证失败时，明确记录原因；必要时回滚。
- 手工操作与定时任务不会并发修改配置。
- 订阅切换或更新后，不使用过期报告覆盖新配置。
